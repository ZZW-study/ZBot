# ZBot 技能库

本目录包含 ZBot 的内置技能。

## 技能目录规范

每个技能都是一个独立目录，目录名必须与 `SKILL.md` frontmatter 中的 `name:` 完全一致。

示例：

```text
github/
├── SKILL.md
├── scripts/        # 可选
├── references/     # 可选
└── assets/         # 可选
```
