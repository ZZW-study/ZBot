# 工具使用说明

工具签名会通过函数调用自动提供。
本文档记录非显而易见的约束条件与使用模式。

## exec — 安全限制
- 命令设有可配置的超时时间（默认 60 秒）
- 危险命令已被屏蔽（rm -rf、format、dd、shutdown 等）
- 输出内容会在 10000 字符处截断
- 可通过 `restrictToWorkspace` 配置将文件访问限制在工作区内

## cron — 定时提醒
- 使用方式请参考 cron 技能文档。